import state from '../candidate/state'
import getters from '../candidate/getters'
import actions from '../candidate/actions'
import mutations from '../candidate/mutations'

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations
}
